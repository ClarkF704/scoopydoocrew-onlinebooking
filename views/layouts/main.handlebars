<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="description" content="ScoopyDooCrew located in Charlotte, NC and surrounding areas taking care of dog waste needs. Residential & Commercial"
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="icon" href="/img/collar-copy.png" type="image/gif" sizes="16x16">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-beta/css/bootstrap.min.css"
    integrity="sha384-/Y6pD6FV/Vv2HJnA6t+vslU6fwYXjCFtcEpHbNJ0lyAFsXTsjBbfaDjzALeQsN6M" crossorigin="anonymous">
  <link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet"
    integrity="sha384-wvfXpqpZZVQGK6TAh5PVlGOfQNHSoD2xbE+QkPxCAFlNEevoEH3Sl0sibVcOQVnN" crossorigin="anonymous">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/3.5.2/animate.css" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
  <link rel="stylesheet" href="/img/style.css">
  <link rel="preconnect" href="https://fonts.gstatic.com" />
  <link href="https://fonts.googleapis.com/css2?family=Acme&family=Montserrat&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css"
    integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous" />

  <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-datepicker/1.3.0/css/datepicker.css" rel="stylesheet"
    type="text/css" />

  <title>ScoopyDooCrew Charlotte, NC</title>
</head>

<body class="main-body">
  <nav class="navbar navbar-expand-lg navbar-light">
    <a class="navbar-brand" href="/"><img src="/img/collar-copy.png" width="150px;" height="150px;" /></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <div class="mr-auto"></div>
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link" style="font-family: 'Montserrat', sans-serif" href="/services">Services</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" style="font-family: 'Montserrat', sans-serif" href="/about">About Us</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" style="font-family: 'Montserrat', sans-serif" href="/faq">FAQ</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" style="font-family: 'Montserrat', sans-serif" href="/contact">Contact</a>
        </li>
      </ul>
    </div>
  </nav>
  <div>
    {{{body}}}
  </div>

<script src="https://code.jquery.com/jquery-3.3.1.slim.min.js" integrity="sha384-q8i/X+965DzO0rT7abK41JStQIAqVgRVzpbzo5smXKp4YfRvH+8abtTE1Pi6jizo" crossorigin="anonymous"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js" integrity="sha384-UO2eT0CpHqdSJQ6hJty5KVphtPhzWj9WO1clHTMGa3JDZwrnQq4sF86dIHNDz0W1" crossorigin="anonymous"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js" integrity="sha384-JjSmVgyd0p3pXB1rRibZUAYoIIy6OrQ6VrjIEaFf/nJGzIxFDsf4x0xIM+B07jRM" crossorigin="anonymous"></script>
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
  <script>
    $(function () {
      $("#datepicker").datepicker({
        autoclose: true,
        todayHighlight: true
      }).datepicker('update', new Date());
    });

    $(".dropdown-menu button").click(function(){
      var selText = $(this).text();
      $(this).parents('.btn-group').find('.dropdown-toggle').html(selText+' <span class="caret"></span>');
    });

    $( "#optionTest" ).click(function() {
      console.log($("select#optionTest").val())
      var weelkySelect = $("select#optionTest").val()
    });

    function reScheduleAttributeSetterWeekly() {
      const weeklyVal = $("#optionWeekly").val()
      const biMonthlyVal = $("#optionBiMonthly").val()
      const monthlyVal = $("#optionMonthly").val()
      const oneTime = $("#optionOneTime").val()
      if(weeklyVal !== ''){
        console.log(weeklyVal)
        $('#serviceConfirmBtn').html(`<span class="new-class-tester">Confirm $<strong>${weeklyVal} Weekly Service</strong></span>`)
        $( "#serviceConfirmBtn" ).prop( "disabled", false );
        $("#biMonthlyGroupDiv").hide()
        $("#monthlyGroupDiv").hide()
        $("#oneTimer").hide()
      } if(weeklyVal == 0) {
        $("#weeklyGroupDiv").show()
        $("#biMonthlyGroupDiv").show()
        $("#monthlyGroupDiv").show()
        $("#oneTimer").show()
        $( "#serviceConfirmBtn" ).prop( "disabled", true );
        $('#serviceConfirmBtn').html(`<span class="new-class-tester">Please Select Service</span>`)
      }
    }
    function reScheduleAttributeSetterBiMonthly() {
      const weeklyVal = $("#optionWeekly").val()
      const biMonthlyVal = $("#optionBiMonthly").val()
      const monthlyVal = $("#optionMonthly").val()
      const oneTime = $("#optionOneTime").val()
      if(biMonthlyVal !== 0){
        console.log(biMonthlyVal)
        $('#serviceConfirmBtn').html(`<span class="new-class-tester">Confirm $<strong>${biMonthlyVal} Bi-Monthly Service</strong></span>`)
        $( "#serviceConfirmBtn" ).prop( "disabled", false );
        $("#oneTimer").hide()
        $("#weeklyGroupDiv").hide()
        $("#monthlyGroupDiv").hide()
      } if(biMonthlyVal == 0) {
        $("#weeklyGroupDiv").show()
        $("#biMonthlyGroupDiv").show()
        $("#oneTimer").show()
        $("#monthlyGroupDiv").show()
        $( "#serviceConfirmBtn" ).prop( "disabled", true );
        $('#serviceConfirmBtn').html(`<span class="new-class-tester">Please Select Service</span>`)
      }
    }
    function reScheduleAttributeSetterMonthly() {
      const weeklyVal = $("#optionWeekly").val()
      const biMonthlyVal = $("#optionBiMonthly").val()
      const monthlyVal = $("#optionMonthly").val()
      const oneTime = $("#optionOneTime").val()
      if(monthlyVal !== 0){
        console.log(monthlyVal)
        $('#serviceConfirmBtn').html(`<span class="new-class-tester">Confirm $<strong>${monthlyVal} Monthly Service</strong></span>`)
        $( "#serviceConfirmBtn" ).prop( "disabled", false );
        $("#biMonthlyGroupDiv").hide()
        $("#oneTimer").hide()
        $("#weeklyGroupDiv").hide()
      } if(monthlyVal == 0) {
        $("#weeklyGroupDiv").show()
        $("#oneTimer").show()
        $("#biMonthlyGroupDiv").show()
        $("#monthlyGroupDiv").show()
        $( "#serviceConfirmBtn" ).prop( "disabled", true );
        $('#serviceConfirmBtn').html(`<span class="new-class-tester">Please Select Service</span>`)
      }
    }
    function reScheduleAttributeSetterOneTime(){
      const weeklyVal = $("#optionWeekly").val()
      const biMonthlyVal = $("#optionBiMonthly").val()
      const monthlyVal = $("#optionMonthly").val()
      const oneTime = $("#optionOneTime").val()
      if(oneTime !== 0){
        console.log(monthlyVal)
        $('#serviceConfirmBtn').html(`<span class="new-class-tester">Confirm $<strong>${oneTime} One Time Service</strong></span>`)
        $( "#serviceConfirmBtn" ).prop( "disabled", false );
        $("#biMonthlyGroupDiv").hide()
        $("#monthlyGroupDiv").hide()
        $("#weeklyGroupDiv").hide()
      } if(oneTime == 0) {
        $("#weeklyGroupDiv").show()
        $("#biMonthlyGroupDiv").show()
        $("#monthlyGroupDiv").show()
        $( "#serviceConfirmBtn" ).prop( "disabled", true );
        $('#serviceConfirmBtn').html(`<span class="new-class-tester">Please Select Service</span>`)
      }
    }
  </script>

  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/js/bootstrap.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-datepicker/1.3.0/js/bootstrap-datepicker.js"></script>
</body>

</html>

{{!--  if(biMonthlyVal !== ''){
        $('#serviceConfirmBtn').html(`<span class="new-class-tester">Confirm $<strong>${biMonthlyVal}</strong></span>`)
        $( "#serviceConfirmBtn" ).prop( "disabled", false );
        $("#weeklyGroupDiv").hide()
        $("#monthlyGroupDiv").hide()
      } if(biMonthlyVal == '') {
        $("#weeklyGroupDiv").show()
        $("#biMonthlyGroupDiv").show()
        $("#monthlyGroupDiv").show()
        $( "#serviceConfirmBtn" ).prop( "disabled", true );
        $('#serviceConfirmBtn').html(`<span class="new-class-tester">Please Select Service</span>`)
      } --}}